## RabbitMQ configuration
## Serve the management UI and all /api/* calls under /rabbitmq/
## This lets nginx proxy_pass without a rewrite, keeping absolute API paths intact.
management.path_prefix = /rabbitmq
